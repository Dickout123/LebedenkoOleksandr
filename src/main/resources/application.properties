# Server port (optional, 8080 is default)
server.port=8080

# Database connection settings (PostgreSQL)
spring.datasource.url=jdbc:postgresql://localhost:5432/recipes_db
spring.datasource.username=postgres
spring.datasource.password=228605228605

# JPA and Hibernate settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Spring Boot application name (optional)
spring.application.name=recipes-app